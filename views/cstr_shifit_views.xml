<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="cstr_shift_form_view" model="ir.ui.view">
        <field name="name">cstr.shift.form</field>
        <field name="model">cstr.shift</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="employee_id"/>
                        <field name="start_time"/>
                        <field name="end_time"/>
                        <field name="project_id"/>
                        <field name="work_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="cstr_shift_tree_view" model="ir.ui.view">
        <field name="name">cstr.shift.tree</field>
        <field name="model">cstr.shift</field>
        <field name="arch" type="xml">
            <tree>
                <field name="employee_id"/>
                <field name="start_time"/>
                <field name="end_time"/>
                <field name="project_id"/>
                <field name="work_id"/>
            </tree>
        </field>
    </record>

</odoo>
